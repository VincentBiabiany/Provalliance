
						<div id="NotSelect" class="alert alert-danger hide">
							{{ "admin.manager.erreurSalon"|trans({}, "translator") }}

								<span class="closeAlert glyphicon glyphicon-remove"></span>
					 </div>
					 {{ form_start(form, {'attr': {'id': 'createAccount', 'class':'allForm createAccount'} }) }}

							 <div class="form-group">
								 <label class="control-label label col-sm-3 col-xs-12" for="salon"> {{ form_label(form.appelation) }}</label>
								 <div class="col-sm-9 col-xs-12">
									 {{ form_widget(form.appelation, { 'attr': {'class': 'form-control'} }) }}
								 </div>
							 </div>
						 <div id="actionsButtons">
				 			{{ form_end(form) }}
							<a id="form_Suivant" class="btn-black end" href="#">
									{{ "global.next"|trans({}, "translator") }}
							</a>
							<a href="{{ path('createType') }}" class="btn-black precedent">
									{{ "global.back"|trans({}, "translator") }}
							</a>


				 		 </div>

	<script type="text/javascript">

	$('.closeAlert').bind('click', function(){
				$(".alert").css('display','none');
				$(".alert").remove();

	});


    $("#form_Suivant").bind('click', function(){

			ChangeEstate('stepTwo','labelTwo',1,'next');

 						if( !$('#form_appelation').val() ){
								$('#NotSelect').removeClass( "hide" ).addClass( "show" );

											}else{

												ChangeEstate('stepTwo','labelTwo',1);

											$('#CreateFormStep').fadeOut('slow', function(){ //on fait disparaitre l ancien contenu
													 idsalon = $('#form_appelation').val();

																	 $.ajax({
														 				url : "{{path('createAccountS2')}}",
														 				type: 'POST',
														 				data : 'idsalon=' + idsalon,
														 				success: function(html) {
																		      	$('#CreateFormStep').html(html);
							 															$('#CreateFormStep').fadeIn('slow');
														 				}
														 			});
											 });
							}
});
		</script>
