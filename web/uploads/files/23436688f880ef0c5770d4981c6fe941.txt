

OBJECTIF:

 Reconstruire pega en suivant les bonnes pratiques des projets Symfony 
 (Utilisé les repository de Doctrine, utilisé qu'un Bundle, mettre les views/Twigs dans app/ ..)
 
 
ETAPES:

1 - ORM extranet:

	J'ai retiré dans les index de extranet, et fais les liens Foreign Key nécessaire. J'ai renomé dans chanque classe
	des Entity les objet pour avoir des getter /setter au nom cohérent (ex $person au lieu de $idperson)
	
	
2 - Remise en place de la sécurité / login
	
	app/config/secutiry.yml : 
	
	/routing.yml
	
	
	
3 - Recréé l'interface:

  3.1 - Accueil: 
  
  Contoller -> GestionCraBundle: Accueilcontoller ( -> Extend ABase )   L'accueil different selon ROLE_SYSADMIN
  
  Views     -> index.html.twig ( extends base.html.twig )
  
  3.2 - Menu
  
  UI/MenuBuilder/
  
  -> Utilise KnpMenu, créé un menu par le biais d'un service + yaml (/Resources/config/menu.yml)
  -> dans web/css  ->>> nova.css utilisé pour cacher/afficher les sous menus + les images 
  -> common.js -> Ajout de classe css (current) pour montrer les sous-menus ( selon le routing /accueil, /identite ...)
  
  
4 - Les controllers:

Les controllers extends ABase, qui charge le menu + la personne a partir du user ainsi que la date.
Chaque controller utilise des Action..



4.1 -> Identité : on peut ajouter /delete contact info.